// task 1

// for (let i = 1; i <= 10; i++) {
//   console.log(Math.round(Math.random() * 30 + 20));
// }

// task 2

for (let i = 1; i <= 1; i++) {
  //   console.log(Math.round(Math.random() * 255 + 0));
  console.log(
    `rgb(${Math.round(Math.random() * 255 + 0)}, ${Math.round(
      Math.random() * 255 + 0
    )}, ${Math.round(Math.random() * 255 + 0)})`
  );
}
for (let i = 1; i <= 1; i++) {
  //   console.log(Math.round(Math.random() * 255 + 0));
  console.log(
    `RGBA(${Math.round(Math.random() * 255 + 0)}, ${Math.round(
      Math.random() * 255 + 0
    )}, ${Math.round(Math.random() * 255 + 0)},${Math.random().toFixed(1)})`
  );
}
